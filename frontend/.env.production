# Baked into the Vercel production build by Vite automatically.
# Routes all API calls through the Vercel proxy function at /api/[...path].js
# which forwards to API_BASE_URL (set in Vercel dashboard → Environment Variables).
VITE_API_URL=/api
